## elice cloudtrack - [3기-java-study] ##

# Java 복습
    class : 필드 또는 메소드를 포함하는 블록 / 클래스 이름은 파일명과 동일해야 하며 숫자로 시작하거나 공백이 포함되면 안된다.
        class block : class 이름 뒤에 {}를 말함
    method : 어떤 일을 처리하는 실행문들을 모아 놓은 블록
        method block : method() 뒤에 {}를 말함
        main() method의 이름을 바꾸면 안되는 이유 : java.exe가 제일 먼저 찾아서 실행하기 때문에 / 진입점(entry point)

    실행문 : 변수 선언, 값 저장, 메소드 호출에 해당하는 코드 종료 시 ;(세미콜론) 반드시 작성

    String은 int,char,byte등 과 같은 기본 타입이 아닌 클래스 타입이고 String 변수는 참조 변수이다.
     => String 객체가 생성되고 String 변수는 String 객체의 번지로 참조 하게 된다.

    강제형 변환에 따른 데이터 손실에 주의하자.