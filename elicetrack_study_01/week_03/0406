## elice cloudtrack - 0406##

클론코딩 파트 분배
0404 성수낙낙에서 오프라인으로 파트 분배 및 개인 공부 진행
html : 이소정 / 우연정
css : 이석현 / 송현진
js : 임호준 / 민경호 

# JavaScript
    스코프는 코드가 현재 실행되는 환경, context를 의미
    this 포인터, 스코프에 저장된 변수들, 스코프 체인등이 환경에 포함

    객체의 메서드의 경우, 메서드 환경의 this는 해당 객체를 가리키게 된다.
    실행 컨텍스트 혹은 실행 맥락은 자바 스크립티그 실행되는 환경이다.

    - dynamic binding
        함수의 호출 환경에 따라 this는 동적으로 세팅된다.
        this가 환경에 따라 바뀌는 것이 dynamic binding이다
    
    - 화살표 함수 '=>'
        화살표 함수의 this는 정해지면 바꿀 수 없다.

    - closure 
        자바스크립트 클로저는, 함수의 일급 객체 성질을 이용
        함수와 함수가 사용하는 변수들을 저장한 공간을 클로저라 한다.

    - 변수 선언
        var : 재할당 o / 함수 스코프
        let : 재할당 o / 블록 스코프
        const : 재할당 x / 블록 스코프

    - 비동기
        자바스크립트는 다른 멀디스레드 언어와 다르게 비동기 동작을 처리한다.
        자바스크립트 엔진은 비동기 처리를 제공하지 않기 때문에 정해진 함수를 제공하여 활용할 수 있다.

        비동기 코드 처리 모듈은 자바스크립트 엔진 외부에 있으며 event loop / task queue / job queue가 있다
        
    - REST API
        구체적인 내용으로는 요청메서드의 의미, URI 설계, 클라이언트의 상태에 대한 동작 등을 정의
