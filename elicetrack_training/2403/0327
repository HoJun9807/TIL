## elice cloudtrack - 0325 ##

# 가게 DB 구축하기
    INSERT INTO 를 사용하여 데이터의 값을 입력한다. -은 null의 값을 갖는다.

# 가게 DB 수정하기
    UPDATE 와 DELETE를 사용하여 WHERE 조건에 맞게 데이블을 수정한다.

# 발언권이 강한 주주는 누구?
    ORDER BY의 옵션 ASC / DESC 를 사용해서 오름차순 / 내림차순으로 조회할 수 있다.

# 가게 DB 분석하기
    SELECT 조회에서 집계함수를 사용해서 원하는 데이터를 조회 할 수 있다.

# 평균 구하기
    집계 함수는 두 개를 동시에 사용할 수도 있다.

# 판매기록 조회하기
    JOIN이 어디에 중심이 되도록 연결할지를 생각해서 LEFT/RIGHT를 적용한다.

# 인기 있는 물건
    HAVING과 WHERE의 차이를 알고 알맞는 옵션을 사용해야 한다.
        HAVING은 집계 함수를 사용할 수 있지만 WHERE은 사용할 수 없다.