## elice cloudtrack - 0304 ##

# 형 변환 이해하기
    - 문자열을 정수로 변환하기 위해서는 Integer.parsInt() 메서드를 사용할 수 있다.

# break문과 continue문
    - 반복문의 실행은 continue와 break으로 제어할 수 있다.

# 입력받은 배열 역순 출력
    - 변수명[i]은 해당 위치의 값을 출력할 수 있다.

# 욕설 필터링 시스템
    - str.charAt(i) 는 문자열의 i 주소값 해당하는 값을 저장할 수 있게 도와주는 메소드이다.
    - 해당 i값에 대응하는 값을 변수에 입력하고 if문 조건문을 사용하여 조건에 맞는 값을 
    필터링하여 result값에 저장 후 출력할 수 있다.