## elice cloudtrack - 0319 ##

# 파일 디렉토리 이동 명령어 사용하기
    cd ls pwd ls -a를 사용해서 디렉토리를 이동하면서 숨겨진 파일을 찾을 수 있다

# 기초 명령어
    mkdir cp chmod 를 사용해서 디렉토리 생성 파일 복사 권한 부여를 할 수 있다.

# file redirection과 pipe
    grep 정규표현식 검색 / | encode.py 파일을 통해서 문자를 변환 /
    redirection > 사용하여 ans.txt파일에 저장


# 변경점의 일부분만 골라서 커밋하기
    git add => local git 저장소 -> git의 staging상태로 올림
    git commit => staging된 파일을 commit 저장함

    git commit -m "커밋메시지 작성" 파일명

# 병합 중 충돌이 발생했을 때 해결하기
    git branch -v 충돌된 파일 찾기
    git commit -am "asdf" 모든 파일을 자동으로 스테이징하고 커밋메시지와 함께 수행
    git checkout master 작업중 중인 브랜치를 master 브랜치로 변경
    git merge bugfix/spaces-before-name 브랜치의 변경사항을 현재 작업 중인 브랜치에
    병합

# 실수로 삭제한 파일 복원하기
    git log 저장소의 기록 확인
    git reset --hard "" 해쉬값으로 복원 

# 여러 개의 저장소를 생성하기
    git init 새로운 저장소를 만들기 위해 사용 / 기존 프로젝트 디렉토리에서 명령어를
    사용하면 git 저장소로 변환된다.