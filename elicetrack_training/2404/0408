## elice cloudtrack - 0408 ##


# Pagination
    Pagination 기능을 사용한다.

# Pagination 정렬
    OrderBy를 추가해서 오름차순 / 내림차순을 페이지를 추가한다.

# Lombok
    @Lombok을 사용해서 생성자 및 setter / getter를 자동으로 생성해준다.

# JPA Auditing
    BaseEntity 클래스를 사용해서 JPA 데이터베이스에 원하는 컬럼을 추가한다.
    해당 프로젝트 시 BaseEntity는 모든 파일에 적용하기 때문에 Application 기능에 
    @EnableJpaAuditing 추가하여 Auditing 기능을 활성화 한다.

# @Transactional
    예외가 발생했을 때 트랜잭션이 적용되어 앞에 설정한 속성값이 롤백된다.